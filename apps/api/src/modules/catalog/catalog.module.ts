import { Module } from '@nestjs/common';

/**
 * Catalog — serviços, preços, duração.
 * Agregado Service { id, tenantId, name, priceCents, durationMinutes }.
 */
@Module({})
export class CatalogModule {}
