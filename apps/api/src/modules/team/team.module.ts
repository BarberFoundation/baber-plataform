import { Module } from '@nestjs/common';

/**
 * Team — barbeiros, jornadas de trabalho, folgas.
 * Agregado Barber + value object WorkSchedule (jornada semanal).
 */
@Module({})
export class TeamModule {}
